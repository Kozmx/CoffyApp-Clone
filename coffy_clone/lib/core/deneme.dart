import 'package:flutter/material.dart';
class DenemeWidget extends StatelessWidget {
  const DenemeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}